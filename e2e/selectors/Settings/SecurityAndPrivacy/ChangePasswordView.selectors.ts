import enContent from '../../../../locales/languages/en.json';

/**
 * Text selectors for the Change Password view in end-to-end tests.
 * Contains localized text strings used to identify UI elements during automated testing.
 */
export const ChangePasswordViewSelectorsText = {
  CHANGE_PASSWORD: enContent.password_reset.change_password,
};
