import { StyleSheet } from 'react-native';

/**
 * Creates stylesheet for ClaimBanner component
 * @returns {Object} StyleSheet object containing styles for claim banner elements
 */
const styleSheet = () =>
  StyleSheet.create({
    claimButton: {
      alignSelf: 'flex-start',
    },
  });

export default styleSheet;
