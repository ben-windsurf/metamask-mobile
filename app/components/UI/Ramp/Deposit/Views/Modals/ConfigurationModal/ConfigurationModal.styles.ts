import { StyleSheet } from 'react-native';

/**
 * Creates stylesheet for the ConfigurationModal component
 * @returns {Object} StyleSheet object containing component styles
 */
const styleSheet = () =>
  StyleSheet.create({
    container: {
      paddingTop: 6,
    },
  });

export default styleSheet;
