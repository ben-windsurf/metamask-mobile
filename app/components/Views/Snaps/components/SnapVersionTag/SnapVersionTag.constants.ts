///: BEGIN:ONLY_INCLUDE_IF(external-snaps)
/**
 * Test ID constant for the snap version badge component
 * Used for identifying the snap version badge in automated tests
 */
export const SNAP_VERSION_BADGE = 'snap-version-badge';

/**
 * Test ID constant for the snap version badge value element
 * Used for identifying the version text within the snap version badge in automated tests
 */
export const SNAP_VERSION_BADGE_VALUE = 'snap-version-badge-value';
///: END:ONLY_INCLUDE_IF
