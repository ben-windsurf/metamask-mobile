///: BEGIN:ONLY_INCLUDE_IF(external-snaps)
/**
 * Test ID constant for snap permissions date element
 * Used for identifying the date display in snap permission views during testing
 */
export const SNAP_PERMISSIONS_DATE = 'snap-permissions-date';

/**
 * Test ID constant for snap permission cell component
 * Used for identifying individual permission cells in snap permission lists during testing
 */
export const SNAP_PERMISSION_CELL = 'snap-permission-cell';

/**
 * Test ID constant for snap permissions title element
 * Used for identifying the title section in snap permission views during testing
 */
export const SNAP_PERMISSIONS_TITLE = 'snap-permissions-title';
///: END:ONLY_INCLUDE_IF
