/**
 * This migration was removed in a previous commit. This file is being preserved
 * so that migrations do not fail, but it intentionally does not do anything.
 *
 * @param {unknown} state - The Redux state to migrate
 * @returns {unknown} The migrated state (unchanged in this case)
 */
export default function migrate(state: unknown) {
  return state;
}
