/**
 * Determines if the network UI redesign feature is enabled.
 * Currently always returns true as the redesign is fully enabled.
 *
 * @returns True indicating the network UI redesign is enabled
 */
export const isNetworkUiRedesignEnabled = () => true;

export default isNetworkUiRedesignEnabled;
