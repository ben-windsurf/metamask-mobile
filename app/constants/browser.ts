/**
 * Constant identifier for external link types in the browser component.
 * Used to distinguish external links from internal navigation links.
 */
export const EXTERNAL_LINK_TYPE = 'external-link';
